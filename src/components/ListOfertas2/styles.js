import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    matches: {
        width: '90%',
        marginTop: 10,
        alignSelf: 'center',
        borderRadius: 8,
        opacity: 0.5,
        backgroundColor: '#000',
    },
});